#!/usr/bin/env sh
# ─────────────────────────────────────────────
# commit-msg — 커밋 메시지 컨벤션 검증 (commitlint)
#   프로젝트 규칙상 본문은 한국어로 작성하되, type/scope 는 영문 소문자를 유지한다.
#   예) feat(todo): 할 일 완료 토글 API 추가
# ─────────────────────────────────────────────
npx --no -- commitlint --edit "$1"
